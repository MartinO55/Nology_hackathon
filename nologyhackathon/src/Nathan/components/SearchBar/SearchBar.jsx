import React from "react";

const SearchBar = () => {
    return (
        <form action="">
            <input type="text" />
            <button></button>
        </form>
    );
};

export default SearchBar;
